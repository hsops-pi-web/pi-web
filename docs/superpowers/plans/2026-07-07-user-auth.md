# pi-web-auth 用户认证与目录隔离 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: 若执行环境提供 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，用其逐任务实施；否则按普通逐任务执行即可。Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 pi-web-auth 增加用户注册/登录、按用户名隔离目录、以及在自己目录内删除文件的能力，独立运行在 8033 端口。

**Architecture:** 在现有 Next.js 16 (App Router) 之外加一层认证。`better-sqlite3` 存 users/sessions（DB 文件 `~/.pi-web-auth/auth.db`），HttpOnly cookie 保持登录。middleware 做 cookie 存在性初筛，各 API 用 `getSessionUser` 做真正校验并把所有文件/会话/agent 操作限制在 `~/pi-users/<用户名>` 内。

**Tech Stack:** Next.js 16, React 19, TypeScript, better-sqlite3, Node 内置 crypto (scrypt), Node 内置 test runner (`node --test`)。

## Global Constraints

- 端口：8033（package.json 的 `dev`/`start` 用 `-p 8033`）。原 8000 的 pi-web 不改动。
- 用户名规则：`^[a-zA-Z][a-zA-Z0-9]*$`（字母开头，允许字母和数字，区分大小写）。
- 密码规则：`^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$`（≥8 且含大小写字母和数字）。
- 注册关键词：环境变量 `REGISTER_KEYWORD`，默认 `tsingmao`，绝不进前端源码。
- 用户目录根：`~/pi-users/<用户名>` 绝对路径。新用户目录仅建空目录。
- DB 文件：`~/.pi-web-auth/auth.db`。
- session cookie：`pi_auth`，HttpOnly + SameSite=Lax + Path=/ + Max-Age=604800（7 天）。
- 越权响应：session/agent 详情按 cwd 归属校验，越权返 **404**（不泄露 id 存在性）；文件路径越权返 403。
- 所有路径校验：用 `realpathSync` 解析后再比对，防 symlink 逃逸。已存在路径（文件读取、session/agent cwd）用 `resolveExistingAndCheck`（解析 target 本身）；待删/待建路径用 `resolveParentAndCheck`（只解析父目录）。解析失败一律 fail-closed 拒绝。
- 登录失败统一返「用户名或密码错误」，不区分用户是否存在。
- 每个改动的 API 若无有效 session 一律 401。
- Agent 工具层隔离本次不实现（见 spec §10），仅 Web UI 层隔离。
- 测试用 Node 内置 test runner：`node --test`。纯逻辑函数（校验、路径判断）必须单测。
- 构建时机（AGENTS 约束）：中间验证阶段一律用 `npm run dev`，**不要**跑 `next build`；仅 Task 18 生产部署阶段才 `npm run build`。

---

## File Structure

新建：
- `lib/auth/db.ts` — SQLite 单例 + 建表。
- `lib/auth/password.ts` — scrypt 哈希与校验。
- `lib/auth/validate.ts` — 用户名/密码正则校验（纯函数，易测）。
- `lib/auth/paths.ts` — `getUserRoot`、`isInsideUserRoot`（纯逻辑）、`resolveExistingAndCheck`、`resolveParentAndCheck`（realpath 校验）。
- `lib/client-auth-fetch.ts` — `authFetch()`：包装 fetch，响应 401 时跳 `/login`（Task 16 引入，覆盖关键路径；全量替换见 Task 19）。
- `lib/auth/session.ts` — `getSessionUser(request)`、`createSession`、`destroySession`。
- `app/api/auth/register-gate/route.ts`
- `app/api/auth/register/route.ts`
- `app/api/auth/login/route.ts`
- `app/api/auth/logout/route.ts`
- `app/api/auth/me/route.ts`
- `app/login/page.tsx` — 登录/注册页。
- `middleware.ts` — 全局 cookie 初筛。
- `app/api/files/[...path]/delete-helpers.ts` — 删除相关纯逻辑（可测）。
- `__tests__/lib/auth/validate.test.ts`、`paths.test.ts`、`password.test.ts`、`delete-helpers.test.ts`。

修改：
- `package.json` — 端口 8033、加 `test` 脚本、加 `better-sqlite3` 依赖。
- `app/api/sessions/route.ts` — 按用户过滤。
- `app/api/sessions/[id]/route.ts` — GET/PATCH/DELETE 加归属校验。
- `app/api/agent/[id]/route.ts`、`app/api/agent/[id]/events/route.ts` — 加归属校验。
- `app/api/agent/new/route.ts` — cwd 默认值 + 用户目录约束。
- `app/api/files/[...path]/route.ts` — 允许根改为单一用户根 + 新增 DELETE。
- `app/api/default-cwd/route.ts` — 返回 `~/pi-users/<username>`。
- `components/FileExplorer.tsx` — 删除按钮 + 确认弹窗。
- `components/AppShell.tsx` — 顶部显示用户名 + 登出。

---

## Task 1: 项目脚手架（端口、依赖、测试运行器）

**Files:**
- Modify: `package.json`

**Interfaces:**
- Produces: `npm test` 运行 `node --test`；`better-sqlite3` 可 import；端口 8033。

- [ ] **Step 1: 安装依赖**

Run:
```bash
cd /home/hsops/pi-web-auth
npm install better-sqlite3
npm install --save-dev @types/better-sqlite3
```
Expected: 安装成功，`node_modules/better-sqlite3` 存在。

- [ ] **Step 2: 改 package.json 端口与测试脚本**

把 `scripts` 改为（仅动 dev/start，新增 test）：
```json
"scripts": {
  "dev": "next dev -p 8033",
  "build": "next build --webpack",
  "start": "next start -p 8033",
  "lint": "eslint .",
  "test": "node --test",
  "release": "npm version patch --no-git-tag-version && npm run build && npm publish --access public"
}
```

- [ ] **Step 3: 验证 better-sqlite3 可用**

Run:
```bash
node -e "const D=require('better-sqlite3'); const db=new D(':memory:'); db.exec('CREATE TABLE t(x)'); console.log('ok');"
```
Expected: 打印 `ok`（原生模块编译并加载成功）。

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: 端口改8033,加better-sqlite3与test脚本"
```

---

## Task 2: 用户名/密码校验（纯函数 + TDD）

**Files:**
- Create: `lib/auth/validate.ts`
- Test: `__tests__/lib/auth/validate.test.ts`

**Interfaces:**
- Produces:
  - `isValidUsername(name: string): boolean`
  - `isValidPassword(pw: string): boolean`

- [ ] **Step 1: 写失败测试**

`__tests__/lib/auth/validate.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { isValidUsername, isValidPassword } from "../../../lib/auth/validate.ts";

test("username: valid", () => {
  for (const n of ["abc", "user1", "Alice", "a", "aZ9"]) {
    assert.equal(isValidUsername(n), true, n);
  }
});

test("username: invalid", () => {
  for (const n of ["1abc", "中文", "a_b", "a-b", "", "a b", "_x", "9"]) {
    assert.equal(isValidUsername(n), false, n);
  }
});

test("password: valid", () => {
  for (const p of ["abcABC12", "Passw0rd", "aaAA0000"]) {
    assert.equal(isValidPassword(p), true, p);
  }
});

test("password: invalid", () => {
  for (const p of ["short1A", "alllower1", "ALLUPPER1", "NoDigitsAA", "12345678", ""]) {
    assert.equal(isValidPassword(p), false, p);
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL（找不到模块 `lib/auth/validate.ts`）。

- [ ] **Step 3: 写实现**

`lib/auth/validate.ts`:
```ts
const USERNAME_RE = /^[a-zA-Z][a-zA-Z0-9]*$/;
const PASSWORD_RE = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;

export function isValidUsername(name: string): boolean {
  return typeof name === "string" && USERNAME_RE.test(name);
}

export function isValidPassword(pw: string): boolean {
  return typeof pw === "string" && PASSWORD_RE.test(pw);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS，全部用例通过。

- [ ] **Step 5: Commit**

```bash
git add lib/auth/validate.ts __tests__/lib/auth/validate.test.ts
git commit -m "feat: 用户名与密码校验纯函数"
```

---

## Task 3: 密码哈希（scrypt, TDD）

**Files:**
- Create: `lib/auth/password.ts`
- Test: `__tests__/lib/auth/password.test.ts`

**Interfaces:**
- Produces:
  - `hashPassword(pw: string): string`  返回 `<saltHex>:<hashHex>`
  - `verifyPassword(pw: string, stored: string): boolean`

- [ ] **Step 1: 写失败测试**

`__tests__/lib/auth/password.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { hashPassword, verifyPassword } from "../../../lib/auth/password.ts";

test("hash then verify roundtrip", () => {
  const stored = hashPassword("Passw0rd");
  assert.match(stored, /^[0-9a-f]+:[0-9a-f]+$/);
  assert.equal(verifyPassword("Passw0rd", stored), true);
  assert.equal(verifyPassword("wrongPass1", stored), false);
});

test("different salts produce different hashes", () => {
  assert.notEqual(hashPassword("Passw0rd"), hashPassword("Passw0rd"));
});

test("malformed stored string returns false", () => {
  assert.equal(verifyPassword("Passw0rd", "garbage"), false);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL（找不到 `lib/auth/password.ts`）。

- [ ] **Step 3: 写实现**

`lib/auth/password.ts`:
```ts
import { randomBytes, scryptSync, timingSafeEqual } from "crypto";

export function hashPassword(pw: string): string {
  const salt = randomBytes(16);
  const hash = scryptSync(pw, salt, 64);
  return `${salt.toString("hex")}:${hash.toString("hex")}`;
}

export function verifyPassword(pw: string, stored: string): boolean {
  const [saltHex, hashHex] = stored.split(":");
  if (!saltHex || !hashHex) return false;
  const salt = Buffer.from(saltHex, "hex");
  const expected = Buffer.from(hashHex, "hex");
  const actual = scryptSync(pw, salt, expected.length);
  return expected.length === actual.length && timingSafeEqual(expected, actual);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add lib/auth/password.ts __tests__/lib/auth/password.test.ts
git commit -m "feat: scrypt密码哈希与校验"
```

---

## Task 4: 用户目录路径逻辑（TDD）

**Files:**
- Create: `lib/auth/paths.ts`
- Test: `__tests__/lib/auth/paths.test.ts`

**Interfaces:**
- Consumes: `os.homedir`, `fs.realpathSync`, `path`
- Produces:
  - `getUserRoot(username: string): string`  → `<home>/pi-users/<username>`
  - `isInsideUserRoot(target: string, username: string): boolean`  纯路径包含判断（不解析 symlink，供单测）
  - `resolveExistingAndCheck(target: string, username: string): boolean`  对**已存在**路径 `realpathSync(target)` 解析后判断；解析失败（路径不存在/坏链）→ fail-closed 返 false。运行期用于：文件 GET、session cwd、agent cwd。
  - `resolveParentAndCheck(target: string, username: string): boolean`  对 target **父目录** realpath 后判断（target 本身可能是待删 symlink 或待建新路径）。运行期用于：删除、新建路径。

- [ ] **Step 1: 写失败测试**

`__tests__/lib/auth/paths.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import os from "os";
import path from "path";
import { mkdtempSync, mkdirSync, writeFileSync, symlinkSync, rmSync } from "fs";
import {
  getUserRoot, isInsideUserRoot, resolveExistingAndCheck, resolveParentAndCheck,
} from "../../../lib/auth/paths.ts";

test("getUserRoot builds home/pi-users/<name>", () => {
  assert.equal(getUserRoot("alice"), path.join(os.homedir(), "pi-users", "alice"));
});

test("isInsideUserRoot: inside", () => {
  const root = getUserRoot("alice");
  assert.equal(isInsideUserRoot(root, "alice"), true);
  assert.equal(isInsideUserRoot(path.join(root, "a/b.txt"), "alice"), true);
});

test("isInsideUserRoot: outside", () => {
  assert.equal(isInsideUserRoot("/etc/passwd", "alice"), false);
  assert.equal(isInsideUserRoot(getUserRoot("bob"), "alice"), false);
  // 前缀相似但非子路径
  assert.equal(isInsideUserRoot(getUserRoot("alice") + "-x", "alice"), false);
});

test("resolveExistingAndCheck follows symlink to real location", () => {
  // 用真实用户根做隔离测试：在 <root>/u 下建普通文件与逃逸 symlink
  const user = "auttest";
  const root = getUserRoot(user);
  mkdirSync(root, { recursive: true });
  const outside = mkdtempSync(path.join(os.tmpdir(), "outside-"));
  try {
    const good = path.join(root, "good.txt");
    writeFileSync(good, "x");
    assert.equal(resolveExistingAndCheck(good, user), true);

    const outsideFile = path.join(outside, "secret.txt");
    writeFileSync(outsideFile, "s");
    const escape = path.join(root, "escape");
    symlinkSync(outsideFile, escape);
    // 跟随 symlink 后落在用户根外 → false
    assert.equal(resolveExistingAndCheck(escape, user), false);
    // 不存在的路径 → fail-closed false
    assert.equal(resolveExistingAndCheck(path.join(root, "nope"), user), false);
  } finally {
    rmSync(root, { recursive: true, force: true });
    rmSync(outside, { recursive: true, force: true });
  }
});

test("resolveParentAndCheck checks parent, not symlink target", () => {
  const user = "auttest2";
  const root = getUserRoot(user);
  mkdirSync(root, { recursive: true });
  try {
    // 待删 symlink：父目录在根内 → true（删除时只删链接本身，不跟随）
    const link = path.join(root, "link");
    symlinkSync("/etc/hosts", link);
    assert.equal(resolveParentAndCheck(link, user), true);
    // 父目录在根外 → false
    assert.equal(resolveParentAndCheck("/etc/newfile", user), false);
  } finally {
    rmSync(root, { recursive: true, force: true });
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL（找不到 `lib/auth/paths.ts`）。

- [ ] **Step 3: 写实现**

`lib/auth/paths.ts`:
```ts
import os from "os";
import path from "path";
import { realpathSync } from "fs";

export function getUserRoot(username: string): string {
  return path.join(os.homedir(), "pi-users", username);
}

export function isInsideUserRoot(target: string, username: string): boolean {
  const root = getUserRoot(username);
  const normalized = path.resolve(target);
  if (normalized === root) return true;
  return normalized.startsWith(root + path.sep);
}

// 已存在路径：对 target 本身 realpath 解析后判断（跟随 symlink 到真实位置）。
// 用于文件读取/列表、session cwd、agent cwd 等目标必然存在的场景。
// 解析失败（不存在/坏链）时 fail-closed 返 false，安全侧默认拒绝。
export function resolveExistingAndCheck(target: string, username: string): boolean {
  try {
    return isInsideUserRoot(realpathSync(target), username);
  } catch {
    return false;
  }
}

// 待删/待建路径：只解析父目录 realpath 后判断。
// target 本身可能是待删的 symlink（不能跟随，否则会判到链接目标）或尚不存在的新路径。
export function resolveParentAndCheck(target: string, username: string): boolean {
  const abs = path.resolve(target);
  const parent = path.dirname(abs);
  let resolvedParent: string;
  try {
    resolvedParent = realpathSync(parent);
  } catch {
    return false; // 父目录不存在 → 拒绝
  }
  return isInsideUserRoot(path.join(resolvedParent, path.basename(abs)), username);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add lib/auth/paths.ts __tests__/lib/auth/paths.test.ts
git commit -m "feat: 用户目录路径逻辑与归属判断"
```

---

## Task 5: SQLite 单例与建表

**Files:**
- Create: `lib/auth/db.ts`

**Interfaces:**
- Consumes: `better-sqlite3`, `os.homedir`, `fs.mkdirSync`
- Produces: `getDb(): Database`（进程内单例，已建 users/sessions 表）。表结构见 spec §4。

- [ ] **Step 1: 写实现**

`lib/auth/db.ts`:
```ts
import Database from "better-sqlite3";
import os from "os";
import path from "path";
import { mkdirSync } from "fs";

declare global {
  // eslint-disable-next-line no-var
  var __piAuthDb: Database.Database | undefined;
}

export function getDb(): Database.Database {
  if (globalThis.__piAuthDb) return globalThis.__piAuthDb;
  const dir = path.join(os.homedir(), ".pi-web-auth");
  mkdirSync(dir, { recursive: true });
  const db = new Database(path.join(dir, "auth.db"));
  db.pragma("journal_mode = WAL");
  db.exec(`
    CREATE TABLE IF NOT EXISTS users (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      username TEXT UNIQUE NOT NULL,
      password_hash TEXT NOT NULL,
      created_at TEXT NOT NULL
    );
    CREATE TABLE IF NOT EXISTS sessions (
      token TEXT PRIMARY KEY,
      username TEXT NOT NULL,
      expires_at INTEGER NOT NULL
    );
  `);
  globalThis.__piAuthDb = db;
  return db;
}
```

- [ ] **Step 2: 冒烟验证**

Run:
```bash
node --input-type=module -e "
import { getDb } from './lib/auth/db.ts';
const db = getDb();
db.prepare('INSERT OR IGNORE INTO users(username,password_hash,created_at) VALUES(?,?,?)').run('smoke','x:y',new Date().toISOString());
console.log('users:', db.prepare('SELECT count(*) c FROM users').get());
"
```
Expected: 打印 users 计数 ≥ 1，`~/.pi-web-auth/auth.db` 生成。

- [ ] **Step 3: 清理冒烟数据并提交**

Run:
```bash
node -e "const D=require('better-sqlite3');const p=require('path').join(require('os').homedir(),'.pi-web-auth','auth.db');const db=new D(p);db.prepare('DELETE FROM users WHERE username=?').run('smoke');console.log('cleaned');"
git add lib/auth/db.ts
git commit -m "feat: SQLite单例与users/sessions建表"
```

---

## Task 6: session 工具（创建/查询/销毁）

**Files:**
- Create: `lib/auth/session.ts`

**Interfaces:**
- Consumes: `getDb` (Task 5), `crypto.randomBytes`
- Produces:
  - `createSession(username: string): string`  返回 token，写库，7 天过期
  - `getSessionUser(request: Request): string | null`  读 `pi_auth` cookie → 校验未过期 → 返 username，过期删行返 null
  - `destroySession(token: string): void`
  - `readCookieToken(request: Request): string | null`
  - `SESSION_COOKIE = "pi_auth"`，`SESSION_MAX_AGE_SEC = 604800`

- [ ] **Step 1: 写实现**

`lib/auth/session.ts`:
```ts
import { randomBytes } from "crypto";
import { getDb } from "./db.ts";

export const SESSION_COOKIE = "pi_auth";
export const SESSION_MAX_AGE_SEC = 604800; // 7 天

export function createSession(username: string): string {
  const token = randomBytes(32).toString("hex");
  const expiresAt = Date.now() + SESSION_MAX_AGE_SEC * 1000;
  getDb().prepare("INSERT INTO sessions(token,username,expires_at) VALUES(?,?,?)")
    .run(token, username, expiresAt);
  return token;
}

export function destroySession(token: string): void {
  getDb().prepare("DELETE FROM sessions WHERE token=?").run(token);
}

export function readCookieToken(request: Request): string | null {
  const header = request.headers.get("cookie");
  if (!header) return null;
  for (const part of header.split(";")) {
    const [k, ...v] = part.trim().split("=");
    if (k === SESSION_COOKIE) return decodeURIComponent(v.join("="));
  }
  return null;
}

export function getSessionUser(request: Request): string | null {
  const token = readCookieToken(request);
  if (!token) return null;
  const row = getDb().prepare("SELECT username,expires_at FROM sessions WHERE token=?")
    .get(token) as { username: string; expires_at: number } | undefined;
  if (!row) return null;
  if (row.expires_at < Date.now()) {
    destroySession(token);
    return null;
  }
  return row.username;
}
```

- [ ] **Step 2: 冒烟验证**

Run:
```bash
node --input-type=module -e "
import { createSession, getSessionUser, destroySession } from './lib/auth/session.ts';
const t = createSession('alice');
const req = new Request('http://x', { headers: { cookie: 'pi_auth='+t } });
console.log('user:', getSessionUser(req));
destroySession(t);
console.log('after destroy:', getSessionUser(req));
"
```
Expected: 先打印 `user: alice`，再打印 `after destroy: null`。

- [ ] **Step 3: Commit**

```bash
git add lib/auth/session.ts
git commit -m "feat: session创建查询销毁工具"
```

---

## Task 7: 注册关键词门 + 注册 API

**Files:**
- Create: `app/api/auth/register-gate/route.ts`, `app/api/auth/register/route.ts`

**Interfaces:**
- Consumes: `isValidUsername`/`isValidPassword` (Task 2), `hashPassword` (Task 3), `getUserRoot` (Task 4), `getDb` (Task 5)
- Produces:
  - `POST /api/auth/register-gate {keyword}` → 200 `{ok:true}` 或 403
  - `POST /api/auth/register {keyword,username,password}` → 200 或 400/403/409

- [ ] **Step 1: register-gate 实现**

`app/api/auth/register-gate/route.ts`:
```ts
import { NextResponse } from "next/server";

export async function POST(req: Request) {
  const { keyword } = await req.json() as { keyword?: string };
  const expected = process.env.REGISTER_KEYWORD ?? "tsingmao";
  if (keyword !== expected) {
    return NextResponse.json({ error: "关键词错误" }, { status: 403 });
  }
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: register 实现**

`app/api/auth/register/route.ts`:
```ts
import { NextResponse } from "next/server";
import { mkdirSync } from "fs";
import { isValidUsername, isValidPassword } from "@/lib/auth/validate";
import { hashPassword } from "@/lib/auth/password";
import { getUserRoot } from "@/lib/auth/paths";
import { getDb } from "@/lib/auth/db";

export async function POST(req: Request) {
  const { keyword, username, password } = await req.json() as {
    keyword?: string; username?: string; password?: string;
  };
  const expected = process.env.REGISTER_KEYWORD ?? "tsingmao";
  if (keyword !== expected) {
    return NextResponse.json({ error: "关键词错误" }, { status: 403 });
  }
  if (!username || !isValidUsername(username)) {
    return NextResponse.json({ error: "用户名不合法：字母开头，仅允许字母和数字" }, { status: 400 });
  }
  if (!password || !isValidPassword(password)) {
    return NextResponse.json({ error: "密码不合法：至少8位，含大小写字母和数字" }, { status: 400 });
  }
  const db = getDb();
  const exists = db.prepare("SELECT 1 FROM users WHERE username=?").get(username);
  if (exists) {
    return NextResponse.json({ error: "用户名已存在" }, { status: 409 });
  }
  db.prepare("INSERT INTO users(username,password_hash,created_at) VALUES(?,?,?)")
    .run(username, hashPassword(password), new Date().toISOString());
  mkdirSync(getUserRoot(username), { recursive: true });
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 3: 手动验证**

先临时启动 dev（后台）：`npm run dev &`，等待就绪后：
```bash
curl -s -X POST localhost:8033/api/auth/register-gate -H 'content-type: application/json' -d '{"keyword":"wrong"}' -w ' [%{http_code}]\n'
curl -s -X POST localhost:8033/api/auth/register-gate -H 'content-type: application/json' -d '{"keyword":"tsingmao"}' -w ' [%{http_code}]\n'
curl -s -X POST localhost:8033/api/auth/register -H 'content-type: application/json' -d '{"keyword":"tsingmao","username":"alice","password":"Passw0rd"}' -w ' [%{http_code}]\n'
curl -s -X POST localhost:8033/api/auth/register -H 'content-type: application/json' -d '{"keyword":"tsingmao","username":"alice","password":"Passw0rd"}' -w ' [%{http_code}]\n'
```
Expected: 依次 403、200 `{ok}`、200 `{ok}`、409。且 `~/pi-users/alice` 目录已建。

- [ ] **Step 4: Commit**

```bash
git add app/api/auth/register-gate/route.ts app/api/auth/register/route.ts
git commit -m "feat: 注册关键词门与注册API"
```

---

## Task 8: 登录/登出/me API

**Files:**
- Create: `app/api/auth/login/route.ts`, `app/api/auth/logout/route.ts`, `app/api/auth/me/route.ts`

**Interfaces:**
- Consumes: `verifyPassword` (Task 3), `createSession`/`getSessionUser`/`destroySession`/`readCookieToken`/`SESSION_COOKIE`/`SESSION_MAX_AGE_SEC` (Task 6), `getDb` (Task 5)
- Produces:
  - `POST /api/auth/login {username,password}` → 200 + Set-Cookie 或 401
  - `POST /api/auth/logout` → 200 + 清 cookie
  - `GET /api/auth/me` → 200 `{username}` 或 401

- [ ] **Step 1: login 实现**

`app/api/auth/login/route.ts`:
```ts
import { NextResponse } from "next/server";
import { verifyPassword } from "@/lib/auth/password";
import { createSession, SESSION_COOKIE, SESSION_MAX_AGE_SEC } from "@/lib/auth/session";
import { getDb } from "@/lib/auth/db";

export async function POST(req: Request) {
  const { username, password } = await req.json() as { username?: string; password?: string };
  const fail = () => NextResponse.json({ error: "用户名或密码错误" }, { status: 401 });
  if (!username || !password) return fail();
  const row = getDb().prepare("SELECT password_hash FROM users WHERE username=?")
    .get(username) as { password_hash: string } | undefined;
  if (!row || !verifyPassword(password, row.password_hash)) return fail();

  const token = createSession(username);
  const res = NextResponse.json({ ok: true });
  res.cookies.set(SESSION_COOKIE, token, {
    httpOnly: true, sameSite: "lax", path: "/", maxAge: SESSION_MAX_AGE_SEC,
  });
  return res;
}
```

- [ ] **Step 2: logout 实现**

`app/api/auth/logout/route.ts`:
```ts
import { NextResponse } from "next/server";
import { destroySession, readCookieToken, SESSION_COOKIE } from "@/lib/auth/session";

export async function POST(req: Request) {
  const token = readCookieToken(req);
  if (token) destroySession(token);
  const res = NextResponse.json({ ok: true });
  res.cookies.set(SESSION_COOKIE, "", { httpOnly: true, path: "/", maxAge: 0 });
  return res;
}
```

- [ ] **Step 3: me 实现**

`app/api/auth/me/route.ts`:
```ts
import { NextResponse } from "next/server";
import { getSessionUser } from "@/lib/auth/session";

export async function GET(req: Request) {
  const username = getSessionUser(req);
  if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
  return NextResponse.json({ username });
}
```

- [ ] **Step 4: 手动验证**

dev 运行中（alice 已在 Task 7 注册）：
```bash
curl -s -X POST localhost:8033/api/auth/login -c /tmp/cj -H 'content-type: application/json' -d '{"username":"alice","password":"wrongPass1"}' -w ' [%{http_code}]\n'
curl -s -X POST localhost:8033/api/auth/login -c /tmp/cj -H 'content-type: application/json' -d '{"username":"alice","password":"Passw0rd"}' -w ' [%{http_code}]\n'
curl -s localhost:8033/api/auth/me -b /tmp/cj -w ' [%{http_code}]\n'
curl -s -X POST localhost:8033/api/auth/logout -b /tmp/cj -c /tmp/cj -w ' [%{http_code}]\n'
curl -s localhost:8033/api/auth/me -b /tmp/cj -w ' [%{http_code}]\n'
```
Expected: 401、200、200 `{username:"alice"}`、200、401。

- [ ] **Step 5: Commit**

```bash
git add app/api/auth/login/route.ts app/api/auth/logout/route.ts app/api/auth/me/route.ts
git commit -m "feat: 登录登出与me接口"
```

---

## Task 9: middleware 全局拦截

**Files:**
- Create: `middleware.ts`

**Interfaces:**
- Consumes: `SESSION_COOKIE` 常量值 `pi_auth`（middleware 在 Edge runtime，不 import SQLite 相关模块，只读 cookie 是否存在）
- Produces: 未登录页面跳 `/login`，未登录 API 返 401。

- [ ] **Step 1: 写实现**

`middleware.ts`（项目根）:
```ts
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

const PUBLIC_PATHS = new Set<string>([
  "/login",
  "/api/auth/register-gate",
  "/api/auth/register",
  "/api/auth/login",
]);

export function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl;
  if (PUBLIC_PATHS.has(pathname)) return NextResponse.next();

  const hasCookie = req.cookies.has("pi_auth");
  if (hasCookie) return NextResponse.next();

  if (pathname.startsWith("/api/")) {
    return NextResponse.json({ error: "未登录" }, { status: 401 });
  }
  const url = req.nextUrl.clone();
  url.pathname = "/login";
  return NextResponse.redirect(url);
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 2: 手动验证**

重启 dev（middleware 需重启生效）。无 cookie 访问：
```bash
curl -s localhost:8033/api/sessions -w ' [%{http_code}]\n'                     # 期望 401
curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' localhost:8033/       # 期望 307 -> /login
curl -s localhost:8033/login -o /dev/null -w '%{http_code}\n'                  # 期望 200(放行)
```
Expected: `/api/sessions` 401；`/` 重定向到 `/login`；`/login` 放行。

- [ ] **Step 3: Commit**

```bash
git add middleware.ts
git commit -m "feat: middleware全局登录拦截"
```

---

## Task 10: 会话列表按用户过滤

**Files:**
- Modify: `app/api/sessions/route.ts`

**Interfaces:**
- Consumes: `getSessionUser` (Task 6), `resolveExistingAndCheck` (Task 4), `listAllSessions`
- Produces: `GET /api/sessions` 只返回 cwd 在当前用户目录下的会话；未登录 401。

- [ ] **Step 1: 改写 route**

`app/api/sessions/route.ts`:
```ts
import { NextResponse } from "next/server";
import { listAllSessions } from "@/lib/session-reader";
import { getSessionUser } from "@/lib/auth/session";
import { resolveExistingAndCheck } from "@/lib/auth/paths";

export async function GET(req: Request) {
  try {
    const username = getSessionUser(req);
    if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
    const sessions = await listAllSessions();
    // realpath 解析每个 cwd 后判断归属，防 symlink 混入。cwd 不存在的会话被排除。
    const filtered = sessions.filter((s) => s.cwd && resolveExistingAndCheck(s.cwd, username));
    return NextResponse.json({ sessions: filtered });
  } catch (error) {
    return NextResponse.json({ error: String(error) }, { status: 500 });
  }
}
```

- [ ] **Step 2: 手动验证**

用 Task 8 的 alice cookie（`/tmp/cj` 已在登出后失效，重新登录）：
```bash
curl -s -X POST localhost:8033/api/auth/login -c /tmp/cj -H 'content-type: application/json' -d '{"username":"alice","password":"Passw0rd"}' >/dev/null
curl -s localhost:8033/api/sessions -b /tmp/cj -w ' [%{http_code}]\n'
```
Expected: 200，`sessions` 数组（alice 目录下无会话时为空 `[]`，符合隔离）。

- [ ] **Step 3: Commit**

```bash
git add app/api/sessions/route.ts
git commit -m "feat: 会话列表按用户目录过滤"
```

---

## Task 11: 会话详情 GET/PATCH/DELETE 归属校验

**Files:**
- Modify: `app/api/sessions/[id]/route.ts`

**Interfaces:**
- Consumes: `getSessionUser` (Task 6), `resolveExistingAndCheck` (Task 4), `resolveSessionPath`, `SessionManager`
- Produces: 三个方法在越权时返 404，未登录返 401。

- [ ] **Step 1: 加共享校验并接入三方法**

在 `app/api/sessions/[id]/route.ts` 顶部 import 后加一个 helper，并在 GET/PATCH/DELETE 里，取到 `filePath` 之后立即校验。新增 import：
```ts
import { getSessionUser } from "@/lib/auth/session";
import { resolveExistingAndCheck } from "@/lib/auth/paths";
```
在文件内加 helper（放在 imports 之后）：
```ts
async function assertOwned(req: Request, filePath: string): Promise<Response | null> {
  const username = getSessionUser(req);
  if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
  const cwd = SessionManager.open(filePath).getHeader()?.cwd ?? "";
  if (!cwd || !resolveExistingAndCheck(cwd, username)) {
    return NextResponse.json({ error: "Session not found" }, { status: 404 });
  }
  return null;
}
```

在 **GET** 中，`const sm = SessionManager.open(filePath);` 之前插入：
```ts
    const denied = await assertOwned(req, filePath);
    if (denied) return denied;
```

在 **PATCH** 中，`const sm = SessionManager.open(filePath);` 之前插入同样两行（PATCH 的第一个参数名为 `req`）。

在 **DELETE** 中，签名把 `_req: Request` 改为 `req: Request`，并在 `resolveSessionPath` 得到 `filePath` 且非空之后插入：
```ts
    const denied = await assertOwned(req, filePath);
    if (denied) return denied;
```

- [ ] **Step 2: 手动验证（越权 404）**

需要两个用户各自的 session 才能完整测；此步先验证登录态与自有会话可访问、无 cookie 时 middleware 拦截。完整越权用例在 Task 16 端到端测。
```bash
# 无 cookie（middleware 拦截）
curl -s localhost:8033/api/sessions/anyid -w ' [%{http_code}]\n'   # 期望 401
```
Expected: 401。

- [ ] **Step 3: Commit**

```bash
git add app/api/sessions/[id]/route.ts
git commit -m "feat: 会话详情GET/PATCH/DELETE按cwd归属校验"
```

---

## Task 12: agent 详情与 SSE 归属校验

**Files:**
- Modify: `app/api/agent/[id]/route.ts`, `app/api/agent/[id]/events/route.ts`

**Interfaces:**
- Consumes: `getSessionUser` (Task 6), `resolveExistingAndCheck` (Task 4), `resolveSessionPath`, `SessionManager`
- Produces: POST/GET agent 与 SSE 在越权时 404，未登录 401。

- [ ] **Step 1: 改 agent/[id]/route.ts**

新增 import：
```ts
import { getSessionUser } from "@/lib/auth/session";
import { resolveExistingAndCheck } from "@/lib/auth/paths";
```
在 **POST** 与 **GET** 函数体最前面插入归属校验。因为运行中的 session 可能没走 resolveSessionPath，这里统一用 resolveSessionPath 取路径判断：
```ts
  const username = getSessionUser(req);
  if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
  {
    const fp = await resolveSessionPath(id);
    if (!fp) return NextResponse.json({ error: "Session not found" }, { status: 404 });
    const cwd = SessionManager.open(fp).getHeader()?.cwd ?? "";
    if (!resolveExistingAndCheck(cwd, username)) {
      return NextResponse.json({ error: "Session not found" }, { status: 404 });
    }
  }
```
注意 GET 的第一个参数当前是 `_req`，改名为 `req`。POST 已是 `req`。

- [ ] **Step 2: 改 agent/[id]/events/route.ts**

在 `const { id } = await params;` 之后、取 session 之前插入：
```ts
  const username = getSessionUser(req);
  if (!username) return new Response("未登录", { status: 401 });
  {
    const fp = await resolveSessionPath(id);
    if (!fp) return new Response("Session not found", { status: 404 });
    const cwd = SessionManager.open(fp).getHeader()?.cwd ?? "";
    if (!resolveExistingAndCheck(cwd, username)) {
      return new Response("Session not found", { status: 404 });
    }
  }
```
并加 import：
```ts
import { getSessionUser } from "@/lib/auth/session";
import { resolveExistingAndCheck } from "@/lib/auth/paths";
```
（`resolveSessionPath` 与 `SessionManager` 已在该文件 import。）

- [ ] **Step 3: 手动验证**

```bash
curl -s localhost:8033/api/agent/anyid -w ' [%{http_code}]\n'          # 无cookie -> 401(middleware)
curl -s localhost:8033/api/agent/anyid/events -w ' [%{http_code}]\n'   # 无cookie -> 401(middleware)
```
Expected: 均 401。

- [ ] **Step 4: Commit**

```bash
git add app/api/agent/[id]/route.ts app/api/agent/[id]/events/route.ts
git commit -m "feat: agent详情与SSE按cwd归属校验"
```

---

## Task 13: 文件列表/读取限制到用户根 + default-cwd + agent/new 约束

**Files:**
- Modify: `app/api/files/[...path]/route.ts`, `app/api/default-cwd/route.ts`, `app/api/agent/new/route.ts`

**Interfaces:**
- Consumes: `getSessionUser` (Task 6), `getUserRoot`/`isInsideUserRoot`/`resolveExistingAndCheck`/`resolveParentAndCheck` (Task 4)
- Produces:
  - 文件 GET 允许根改为单一用户根（realpath 后校验）；未登录 401；越权 403。
  - `POST /api/default-cwd` 建/返回 `~/pi-users/<username>`。
  - `agent/new` 未传 cwd 默认用户根；传了必须在用户根内。

- [ ] **Step 1: 文件 GET 改允许根**

在 `app/api/files/[...path]/route.ts` 的 `GET` 中，把
```ts
    const allowedRoots = await getAllowedRoots();
    if (!isPathAllowed(filePath, allowedRoots)) {
      return NextResponse.json({ error: "Access denied" }, { status: 403 });
    }
```
替换为：
```ts
    const username = getSessionUser(request);
    if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
    // 读取/列表/download/watch 目标必然存在：realpath 解析 target 本身后校验，防 symlink 逃逸。
    if (!resolveExistingAndCheck(filePath, username)) {
      return NextResponse.json({ error: "Access denied" }, { status: 403 });
    }
```
并加 import：
```ts
import { getSessionUser } from "@/lib/auth/session";
import { getUserRoot, resolveExistingAndCheck } from "@/lib/auth/paths";
```
换完后 `getAllowedRoots()`、`isPathAllowed()`、`__piAllowedRootsCache` 全局声明与
`ALLOWED_ROOTS_TTL_MS`、`listAllSessions` 相关 import（若仅此处用）成为死代码，**一并删除**：
- 删 `app/api/files/[...path]/route.ts` 里的 `getAllowedRoots` 函数、`isPathAllowed` 函数、
  `declare global { var __piAllowedRootsCache ... }` 块、`ALLOWED_ROOTS_TTL_MS` 常量、
  及仅供它们使用的 import（如 `listAllSessions`）。注意 Task 14 的 DELETE 仍需 `listAllSessions`，
  故实际删除顺序：本任务保留 `listAllSessions` import，Task 14 会用到。
- 删 `app/api/agent/new/route.ts:37` 的 `globalThis.__piAllowedRootsCache?.roots.add(cwd);` 行
  （缓存已不存在）。
（Task 14 的 DELETE 会用 `resolveParentAndCheck`。）

- [ ] **Step 2: default-cwd 改为用户根**

`app/api/default-cwd/route.ts`:
```ts
import { NextResponse } from "next/server";
import { mkdirSync } from "fs";
import { getSessionUser } from "@/lib/auth/session";
import { getUserRoot } from "@/lib/auth/paths";

export async function POST(req: Request) {
  try {
    const username = getSessionUser(req);
    if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });
    const dir = getUserRoot(username);
    mkdirSync(dir, { recursive: true });
    return NextResponse.json({ cwd: dir });
  } catch (error) {
    return NextResponse.json({ error: String(error) }, { status: 500 });
  }
}
```

- [ ] **Step 3: agent/new 加 cwd 约束**

在 `app/api/agent/new/route.ts` 的 POST 中，替换开头的 cwd 处理块。把
```ts
    const body = await req.json() as { cwd?: string; [key: string]: unknown };
    const { cwd: rawCwd, ...command } = body;

    if (!rawCwd || typeof rawCwd !== "string") {
      return NextResponse.json({ error: "cwd is required" }, { status: 400 });
    }
    const cwd = expandHomePath(rawCwd);
    if (!existsSync(cwd)) {
      return NextResponse.json({ error: `Directory does not exist: ${rawCwd}` }, { status: 400 });
    }
```
替换为：
```ts
    const username = getSessionUser(req);
    if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });

    const body = await req.json() as { cwd?: string; [key: string]: unknown };
    const { cwd: rawCwd, ...command } = body;

    const cwd = rawCwd && typeof rawCwd === "string" ? expandHomePath(rawCwd) : getUserRoot(username);
    // 已存在：realpath 解析 target 本身；不存在：解析父目录（允许在用户根内新建）。
    // 两种情形都必须落在用户根内，否则拒绝。防 symlink 子路径逃逸。
    const ok = existsSync(cwd)
      ? resolveExistingAndCheck(cwd, username)
      : resolveParentAndCheck(cwd, username);
    if (!ok) {
      return NextResponse.json({ error: "cwd 必须在你的用户目录内" }, { status: 400 });
    }
    mkdirSync(cwd, { recursive: true });
```
并调整 import：加 `import { mkdirSync } from "fs";`（`existsSync` 已 import，保留），并加：
```ts
import { getSessionUser } from "@/lib/auth/session";
import { getUserRoot, resolveExistingAndCheck, resolveParentAndCheck } from "@/lib/auth/paths";
```

- [ ] **Step 4: 手动验证**

alice 登录后：
```bash
curl -s -X POST localhost:8033/api/default-cwd -b /tmp/cj -w ' [%{http_code}]\n'
# 越权读取别的目录
curl -s "localhost:8033/api/files/etc/passwd?type=read" -b /tmp/cj -w ' [%{http_code}]\n'
# 读取自己目录（列出）
curl -s "localhost:8033/api/files/$(echo $HOME/pi-users/alice | sed 's#/#/#g')?type=list" -b /tmp/cj -w ' [%{http_code}]\n'
```
Expected: default-cwd 200 返回 `~/pi-users/alice`；越权读 `/etc/passwd` 返 403；读自己目录 200。

- [ ] **Step 5: Commit**

```bash
git add app/api/files/[...path]/route.ts app/api/default-cwd/route.ts app/api/agent/new/route.ts
git commit -m "feat: 文件访问/默认cwd/新建会话限制到用户目录"
```

---

## Task 14: 删除逻辑（纯函数 TDD）+ 删除 API

**Files:**
- Create: `app/api/files/[...path]/delete-helpers.ts`
- Test: `__tests__/lib/auth/delete-helpers.test.ts`
- Modify: `app/api/files/[...path]/route.ts`（新增 DELETE 导出）

**Interfaces:**
- Consumes: `getUserRoot`/`resolveParentAndCheck` (Task 4), `getSessionUser` (Task 6), `listAllSessions`
- Produces:
  - `isUserRootItself(target, username): boolean`（纯函数）
  - `isPathInUse(target, sessionCwds: string[]): boolean`（纯函数：target 等于某 cwd 或为其前缀父级/子级）
  - `DELETE /api/files/[...path]` → 200 / 401 / 403 / 200`{needConfirm}`

- [ ] **Step 1: 写删除纯函数的失败测试**

`__tests__/lib/auth/delete-helpers.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import os from "os";
import path from "path";
import { isUserRootItself, isPathInUse } from "../../../app/api/files/[...path]/delete-helpers.ts";

const root = path.join(os.homedir(), "pi-users", "alice");

test("isUserRootItself true only for the root", () => {
  assert.equal(isUserRootItself(root, "alice"), true);
  assert.equal(isUserRootItself(path.join(root, "a.txt"), "alice"), false);
});

test("isPathInUse detects in-use cwd and its subtree", () => {
  const cwds = [path.join(root, "proj")];
  assert.equal(isPathInUse(path.join(root, "proj"), cwds), true);          // 相等
  assert.equal(isPathInUse(path.join(root, "proj", "f.txt"), cwds), true); // 子路径
  assert.equal(isPathInUse(root, cwds), true);                             // 父目录含 cwd
  assert.equal(isPathInUse(path.join(root, "other"), cwds), false);        // 无关
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL（找不到 delete-helpers）。

- [ ] **Step 3: 写纯函数实现**

`app/api/files/[...path]/delete-helpers.ts`:
```ts
import path from "path";
import { getUserRoot } from "@/lib/auth/paths";

export function isUserRootItself(target: string, username: string): boolean {
  return path.resolve(target) === getUserRoot(username);
}

// target 与任一使用中 cwd 存在祖先/后代/相等关系，即视为使用中。
export function isPathInUse(target: string, sessionCwds: string[]): boolean {
  const t = path.resolve(target);
  return sessionCwds.some((raw) => {
    const c = path.resolve(raw);
    if (t === c) return true;
    if (t.startsWith(c + path.sep)) return true; // target 在 cwd 内
    if (c.startsWith(t + path.sep)) return true; // cwd 在 target 内
    return false;
  });
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS。

- [ ] **Step 5: 新增 DELETE 到文件 route**

在 `app/api/files/[...path]/route.ts` 末尾追加 DELETE 导出，并在文件顶部补 import：
```ts
import { lstatSync, unlinkSync, rmSync } from "fs";
import { resolveParentAndCheck } from "@/lib/auth/paths";
import { listAllSessions } from "@/lib/session-reader";
import { isUserRootItself, isPathInUse } from "./delete-helpers";
```
（`getSessionUser`、`getUserRoot`、`resolveExistingAndCheck` 已在 Task 13 import；`fs` 已默认 import 为 `fs`，故用具名再引一次 lstatSync 等即可。）

DELETE 实现：
```ts
export async function DELETE(
  request: NextRequest,
  { params }: { params: Promise<{ path: string[] }> }
) {
  try {
    const username = getSessionUser(request);
    if (!username) return NextResponse.json({ error: "未登录" }, { status: 401 });

    const { path: segments } = await params;
    const filePath = filePathFromSegments(segments);

    if (!resolveParentAndCheck(filePath, username)) {
      return NextResponse.json({ error: "Access denied" }, { status: 403 });
    }
    if (isUserRootItself(filePath, username)) {
      return NextResponse.json({ error: "不能删除用户根目录" }, { status: 403 });
    }

    const force = request.nextUrl.searchParams.get("force") === "1";
    if (!force) {
      const cwds = (await listAllSessions())
        .map((s) => s.cwd)
        .filter((c): c is string => !!c);
      if (isPathInUse(filePath, cwds)) {
        return NextResponse.json({ needConfirm: true, reason: "in_use" });
      }
    }

    let stat;
    try {
      stat = lstatSync(filePath);
    } catch {
      return NextResponse.json({ error: "Not found" }, { status: 404 });
    }

    if (stat.isSymbolicLink()) {
      unlinkSync(filePath);               // 只删链接本身
    } else if (stat.isDirectory()) {
      rmSync(filePath, { recursive: true });
    } else {
      rmSync(filePath);
    }
    return NextResponse.json({ ok: true });
  } catch (error) {
    return NextResponse.json({ error: String(error) }, { status: 500 });
  }
}
```

- [ ] **Step 6: 手动验证删除**

alice 登录后，在其目录建测试文件再删：
```bash
mkdir -p ~/pi-users/alice/sub && echo hi > ~/pi-users/alice/del.txt
ENC="$(echo -n "$HOME/pi-users/alice/del.txt" | sed 's#^/##')"
curl -s -X DELETE "localhost:8033/api/files/$ENC" -b /tmp/cj -w ' [%{http_code}]\n'
ls ~/pi-users/alice/del.txt 2>&1 | head -1
# 越权删除
curl -s -X DELETE "localhost:8033/api/files/etc/hosts" -b /tmp/cj -w ' [%{http_code}]\n'
```
Expected: 删 del.txt 返 200 且文件消失；删 `/etc/hosts` 返 403。

- [ ] **Step 7: Commit**

```bash
git add app/api/files/[...path]/delete-helpers.ts __tests__/lib/auth/delete-helpers.test.ts app/api/files/[...path]/route.ts
git commit -m "feat: 文件删除逻辑与DELETE接口(symlink/使用中/根目录防护)"
```

---

## Task 15: 前端登录/注册页

**Files:**
- Create: `app/login/page.tsx`

**Interfaces:**
- Consumes: `/api/auth/register-gate`, `/api/auth/register`, `/api/auth/login`
- Produces: `/login` 页面，登录成功跳 `/`。

- [ ] **Step 1: 写登录/注册页**

`app/login/page.tsx`:
```tsx
"use client";

import { useState } from "react";

const USERNAME_RE = /^[a-zA-Z][a-zA-Z0-9]*$/;
const PASSWORD_RE = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/;

export default function LoginPage() {
  const [mode, setMode] = useState<"login" | "register">("login");
  const [gatePassed, setGatePassed] = useState(false);
  const [keyword, setKeyword] = useState("");
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");
  const [msg, setMsg] = useState<string | null>(null);

  async function submitGate() {
    setMsg(null);
    const res = await fetch("/api/auth/register-gate", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ keyword }),
    });
    if (res.ok) setGatePassed(true);
    else setMsg("注册关键词错误");
  }

  async function submitRegister() {
    setMsg(null);
    if (!USERNAME_RE.test(username)) return setMsg("用户名：字母开头，仅字母和数字");
    if (!PASSWORD_RE.test(password)) return setMsg("密码：至少8位，含大小写字母和数字");
    const res = await fetch("/api/auth/register", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ keyword, username, password }),
    });
    const data = await res.json();
    if (res.ok) { setMsg("注册成功，请登录"); setMode("login"); setPassword(""); }
    else setMsg(data.error ?? "注册失败");
  }

  async function submitLogin() {
    setMsg(null);
    const res = await fetch("/api/auth/login", {
      method: "POST", headers: { "content-type": "application/json" },
      body: JSON.stringify({ username, password }),
    });
    if (res.ok) window.location.href = "/";
    else setMsg("用户名或密码错误");
  }

  const inputStyle = {
    width: "100%", marginBottom: 8, padding: "6px 8px",
    background: "var(--bg-panel)", color: "var(--text)",
    border: "1px solid var(--border)", borderRadius: 4, fontSize: 13,
  } as const;
  const btnStyle = {
    padding: "6px 12px", background: "var(--accent)", color: "#fff",
    border: "none", borderRadius: 4, cursor: "pointer", fontSize: 13,
  } as const;

  return (
    <div style={{
      maxWidth: 360, margin: "80px auto", padding: 24,
      background: "var(--bg-panel)", border: "1px solid var(--border)",
      borderRadius: 8, color: "var(--text)",
    }}>
      <div style={{ display: "flex", gap: 8, marginBottom: 16 }}>
        <button onClick={() => { setMode("login"); setMsg(null); }} disabled={mode === "login"}
          style={{ ...btnStyle, background: mode === "login" ? "var(--accent)" : "var(--bg-hover)", color: mode === "login" ? "#fff" : "var(--text-dim)" }}>登录</button>
        <button onClick={() => { setMode("register"); setMsg(null); }} disabled={mode === "register"}
          style={{ ...btnStyle, background: mode === "register" ? "var(--accent)" : "var(--bg-hover)", color: mode === "register" ? "#fff" : "var(--text-dim)" }}>注册</button>
      </div>

      {mode === "register" && !gatePassed ? (
        <div>
          <label style={{ fontSize: 12, color: "var(--text-dim)" }}>注册关键词</label>
          <input value={keyword} onChange={(e) => setKeyword(e.target.value)} style={inputStyle} />
          <button onClick={submitGate} style={btnStyle}>验证关键词</button>
        </div>
      ) : (
        <div>
          <label style={{ fontSize: 12, color: "var(--text-dim)" }}>用户名</label>
          <input value={username} onChange={(e) => setUsername(e.target.value)} style={inputStyle} />
          <label style={{ fontSize: 12, color: "var(--text-dim)" }}>密码</label>
          <input type="password" value={password} onChange={(e) => setPassword(e.target.value)} style={inputStyle} />
          {mode === "register" && (
            <div style={{ fontSize: 12, color: "var(--text-dim)", marginBottom: 8 }}>
              至少 8 位，含大小写字母和数字
            </div>
          )}
          <button onClick={mode === "login" ? submitLogin : submitRegister} style={{ ...btnStyle, marginTop: 8 }}>
            {mode === "login" ? "登录" : "提交注册"}
          </button>
        </div>
      )}

      {msg && <div style={{ marginTop: 12, color: "#e5484d", fontSize: 13 }}>{msg}</div>}
    </div>
  );
}
```

注：`--bg-panel`、`--text`、`--text-dim`、`--border`、`--bg-hover`、`--accent` 均为 `app/globals.css` 已定义的变量（见现有组件如 FileExplorer 的用法）。实现前用 `grep -n "^\s*--" app/globals.css` 确认变量名，若某变量名不同则按实际调整。

- [ ] **Step 2: 手动验证（浏览器）**

访问 `http://<服务器>:8033/login`：
- 切到"注册" → 显示关键词框；输错误关键词 → 提示错误、不显示表单；输 `tsingmao` → 展开用户名密码。
- 注册合法用户 → "注册成功，请登录"，切回登录。
- 登录成功 → 跳到 `/`（主界面）。

- [ ] **Step 3: Commit**

```bash
git add app/login/page.tsx
git commit -m "feat: 登录注册页(含注册关键词门)"
```

---

## Task 16: authFetch(关键路径) + 顶部用户名/登出 + 文件删除按钮

**Files:**
- Create: `lib/client-auth-fetch.ts`
- Modify: `components/AppShell.tsx`, `components/FileExplorer.tsx`, `components/SessionSidebar.tsx`, `hooks/useAgentSession.ts`, `components/FileViewer.tsx`, `components/ModelsConfig.tsx`, `lib/agent-client.ts`

**Interfaces:**
- Consumes: `/api/auth/me`, `/api/auth/logout`, `DELETE /api/files/...`
- Produces:
  - `authFetch(input, init?): Promise<Response>` — 包装 fetch，响应 401 时 `location.href="/login"` 并抛出，阻止后续逻辑把 401 当正常数据。
  - 关键路径 fetch 全部改用 authFetch（清单见 Step 1）；顶部显示用户名+登出；文件树每项可删除。
- 说明：本任务只覆盖**关键路径清单**。剩余全部 fetch 的全量替换见 **Task 19**（本期必做）。

- [ ] **Step 1: 新建 authFetch 并替换关键路径**

`lib/client-auth-fetch.ts`:
```ts
// 统一 fetch 包装：任意响应 401 → 跳登录页。用于所有需要登录态的前端请求。
export async function authFetch(input: RequestInfo | URL, init?: RequestInit): Promise<Response> {
  const res = await fetch(input, init);
  if (res.status === 401) {
    window.location.href = "/login";
    throw new Error("unauthorized");
  }
  return res;
}
```

把以下**关键路径**的 `fetch(` 改为 `authFetch(`（各文件顶部加 `import { authFetch } from "@/lib/client-auth-fetch";`）：
- `components/SessionSidebar.tsx`：`/api/sessions`(:207)、`/api/default-cwd`(:282)、session PATCH(:924)、session DELETE(:945)
- `hooks/useAgentSession.ts`：agent state 轮询(:168、:204、:270)、`/api/agent/new`(:387)、`/api/models`(:684)
- `components/FileExplorer.tsx`：`/api/files/...?type=list`(:32)
- `components/FileViewer.tsx`：`/api/files/...?type=read`(:635)
- `components/ModelsConfig.tsx`：`/api/models-config`(:1254、:1346)、`/api/models-config/test`(:529)
- `lib/agent-client.ts`：agent send(:14)、`/api/upload`(:35)

注意：`authFetch` 在 401 时抛错，替换处若原来有 `.catch`/`try` 要保留；纯读取展示类调用（如 SessionSidebar 的 `/api/home`）不在关键清单，留待 Task 19。

行号为当前代码参考，实施时以实际匹配为准。SSE（EventSource）的 401 处理见 Step 2 注记，不在本步。

- [ ] **Step 2: AppShell 顶部加用户名与登出**

在 `components/AppShell.tsx` 组件内加状态与副作用（挑选合适位置，靠近其他 useState/useEffect）。这里直接用原生 fetch 判断（me 的 401 等价于未登录，手动跳转即可）：
```tsx
const [authUser, setAuthUser] = useState<string | null>(null);
useEffect(() => {
  fetch("/api/auth/me")
    .then((r) => (r.ok ? r.json() : null))
    .then((d) => { if (d?.username) setAuthUser(d.username); else window.location.href = "/login"; })
    .catch(() => {});
}, []);
```

SSE 特例注记（EventSource 无法自动跳转）：`hooks/useAgentSession.ts`(:231) 与 `components/FileViewer.tsx`(:331/:466/:682) 的 `EventSource` `onerror` 回调里，补一次探测——`fetch("/api/auth/me")`，若 `res.status===401` 则 `window.location.href="/login"`。这确保 session 过期时 SSE 断开也能跳登录。此注记在 Task 19 完成完整替换，本任务可先只加 useAgentSession 的探测。
在顶部工具栏 JSX 里加一小块（放在现有顶栏容器内）：
```tsx
{authUser && (
  <span style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12 }}>
    <span style={{ color: "var(--text-dim)" }}>{authUser}</span>
    <button
      onClick={async () => { await fetch("/api/auth/logout", { method: "POST" }); window.location.href = "/login"; }}
      style={{ fontSize: 12, cursor: "pointer" }}
    >登出</button>
  </span>
)}
```

- [ ] **Step 3: FileExplorer 加删除按钮**

在 `components/FileExplorer.tsx` 的 `TreeNode` 里，`onAtMention && hovered` 的 mention 按钮相邻处，增加删除按钮。先给 `Props` 与 `TreeNode` 增加一个 `onDeleted` 回调，用于删除后刷新。

在 `FileExplorer` 组件签名不变，内部新增：
```tsx
const [localRefresh, setLocalRefresh] = useState(0);
```
把根 `fetchEntries(cwd)` 的 useEffect 依赖加上 `localRefresh`（与 `refreshKey` 并列），并把 `localRefresh` 通过一个新 prop `bumpRefresh={() => setLocalRefresh((n) => n + 1)}` 传给每个 `TreeNode`。

在 `TreeNode` 内 hovered 时，mention 按钮之后加：
```tsx
{hovered && (
  <button
    onClick={async (e) => {
      e.stopPropagation();
      if (!confirm(`确定删除 ${node.name}？不可恢复`)) return;
      const enc = encodeFilePathForApi(node.fullPath);
      // authFetch 在 401 时自动跳登录并抛错，故用 try 包裹
      try {
        let res = await authFetch(`/api/files/${enc}`, { method: "DELETE" });
        let data = await res.json().catch(() => ({}));
        if (data?.needConfirm) {
          if (!confirm(`${node.name} 正被会话使用，仍要删除？`)) return;
          res = await authFetch(`/api/files/${enc}?force=1`, { method: "DELETE" });
          data = await res.json().catch(() => ({}));
        }
        if (res.ok) bumpRefresh();
        else alert(data?.error ?? "删除失败");
      } catch { /* 401 已跳转 */ }
    }}
    title="删除"
    style={{
      position: "absolute", right: 4, top: "50%", transform: "translateY(-50%)",
      height: 20, padding: "0 6px", background: "var(--bg-panel)",
      border: "1px solid var(--border)", borderRadius: 4, color: "#e5484d",
      cursor: "pointer", fontSize: 11,
    }}
  >删除</button>
)}
```
注意：mention 按钮与删除按钮都绝对定位在 right:4，需将其一改为并排（例如删除按钮 right:4、mention 按钮 right:56），避免重叠。调整 mention 按钮 `right: 4` → `right: 56`。

把 `bumpRefresh` 作为 prop 一路从 `FileExplorer` 传到每个 `TreeNode`（含递归子节点）。
FileExplorer 顶部加 `import { authFetch } from "@/lib/client-auth-fetch";`。

- [ ] **Step 4: 手动验证（浏览器）**

登录后进入主界面：
- 顶部显示用户名，点"登出"回到 `/login`。
- 在文件树 hover 文件 → 出现"删除"按钮 → 点击确认 → 文件消失、树刷新。
- 手动使 cookie 失效（删浏览器 cookie），触发一个关键路径请求（如切换会话拉列表）→ 自动跳 `/login`。

- [ ] **Step 5: Commit**

```bash
git add lib/client-auth-fetch.ts components/AppShell.tsx components/FileExplorer.tsx components/SessionSidebar.tsx hooks/useAgentSession.ts components/FileViewer.tsx components/ModelsConfig.tsx lib/agent-client.ts
git commit -m "feat: authFetch关键路径401跳登录/顶部用户名登出/文件删除按钮"
```

---

## Task 17: 端到端越权验证（两用户）

**Files:**
- 无新增（验证任务）。产出一份手动验证记录，确认隔离生效。

**Interfaces:**
- Consumes: 全部已实现 API。

- [ ] **Step 1: 准备两个用户与各自会话**

dev 运行中。注册 bob，并让 alice、bob 各在自己目录建一个新会话（通过 UI 新建会话，cwd 选各自 `~/pi-users/<name>`），记录各自 sessionId 为 `$AID`、`$BID`。
```bash
curl -s -X POST localhost:8033/api/auth/register -H 'content-type: application/json' \
  -d '{"keyword":"tsingmao","username":"bob","password":"Passw0rd"}' -w ' [%{http_code}]\n'
```

- [ ] **Step 2: 越权矩阵**

alice 登录到 `/tmp/ca`，bob 登录到 `/tmp/cb`。用 alice 的 cookie 访问 bob 的资源：
```bash
curl -s localhost:8033/api/sessions/$BID -b /tmp/ca -w ' [%{http_code}]\n'                 # 期望 404
curl -s localhost:8033/api/sessions/$BID/context -b /tmp/ca -w ' [%{http_code}]\n'          # 期望 404
curl -s -X PATCH localhost:8033/api/sessions/$BID -b /tmp/ca -H 'content-type: application/json' -d '{"name":"x"}' -w ' [%{http_code}]\n'  # 404
curl -s -X DELETE localhost:8033/api/sessions/$BID -b /tmp/ca -w ' [%{http_code}]\n'        # 期望 404
curl -s -X POST localhost:8033/api/agent/$BID -b /tmp/ca -H 'content-type: application/json' -d '{"type":"get_state"}' -w ' [%{http_code}]\n'  # 404
curl -s localhost:8033/api/agent/$BID/events -b /tmp/ca -w ' [%{http_code}]\n'              # 期望 404
# 文件越权
ENCB="$(echo -n "$HOME/pi-users/bob" | sed 's#^/##')"
curl -s "localhost:8033/api/files/$ENCB?type=list" -b /tmp/ca -w ' [%{http_code}]\n'        # 期望 403
```
Expected: 前 6 个 404，文件越权 403。

- [ ] **Step 3: symlink 逃逸验证**

在 alice 目录建指向目录外的 symlink，删除只删链接本身：
```bash
ln -s /etc/hosts ~/pi-users/alice/evil-link
ENCL="$(echo -n "$HOME/pi-users/alice/evil-link" | sed 's#^/##')"
curl -s -X DELETE "localhost:8033/api/files/$ENCL" -b /tmp/ca -w ' [%{http_code}]\n'
ls -l /etc/hosts >/dev/null && echo "target intact"
ls ~/pi-users/alice/evil-link 2>&1 | head -1
```
Expected: 200；`/etc/hosts` 仍在（target intact）；`evil-link` 已删除。

- [ ] **Step 4: 过期/无 cookie 跳转**

```bash
curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' localhost:8033/        # 无cookie -> 307 /login
```
Expected: 307 重定向到 `/login`。

- [ ] **Step 5: 记录结果并提交（若有修正）**

若全部符合预期，无需改代码。如发现问题，回到对应 Task 修复后再来。
```bash
git commit --allow-empty -m "test: 端到端越权验证通过(两用户/symlink/未登录)"
```

---

## Task 18: 生产构建与 systemd 服务

**Files:**
- 无仓库文件改动（服务文件在 `~/.config/systemd/user/`，不入库）。

**Interfaces:**
- Consumes: package.json 端口 8033、`REGISTER_KEYWORD` env。

- [ ] **Step 1: 停止 dev、清理测试数据**

```bash
# 停掉后台 dev
pkill -f "next dev -p 8033" || true
```
（如需保留测试用户可跳过清库；生产前建议清掉 smoke/测试账号。）

- [ ] **Step 2: 生产构建**

Run:
```bash
cd /home/hsops/pi-web-auth
npm run build
```
Expected: 构建成功，生成 `.next/`。

- [ ] **Step 3: 查端口占用，确认 8033 空闲**

Run:
```bash
ss -ltnp | grep -E ':8033|:8000' || echo "8033/8000 空闲情况见上"
```
Expected: 8000 被现有 pi-web 占用（不动），8033 空闲。

- [ ] **Step 4: 创建 systemd 用户服务**

写 `~/.config/systemd/user/pi-web-auth.service`（8000 的 pi-web.service 不改）：
```ini
[Unit]
Description=Pi Agent Web (auth, 8033)
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/hsops/pi-web-auth
ExecStart=/usr/bin/env npm run start
Restart=always
RestartSec=3
Environment=NODE_ENV=production
Environment=REGISTER_KEYWORD=tsingmao

[Install]
WantedBy=default.target
```

- [ ] **Step 5: 启用并验证**

Run:
```bash
systemctl --user daemon-reload
systemctl --user enable --now pi-web-auth
sleep 2
curl -s -o /dev/null -w '%{http_code}\n' localhost:8033/login   # 期望 200
systemctl --user status pi-web-auth --no-pager | head -5
```
Expected: `/login` 返 200；服务 active (running)。原 8000 pi-web 不受影响。

---

## Task 19: authFetch 全量替换剩余 fetch

**背景：** Task 16 只把关键路径的 fetch 换成了 authFetch。此任务把前端**剩余全部** fetch 收口到 authFetch，做到任意请求 401 都跳登录。本期必做，避免"部分请求过期跳登录、部分不跳"的不一致体验。

**Files:**
- Modify: 所有仍在直接用 `fetch(` 的前端文件（下方清单为当前快照，实施前用命令重新枚举）。

**Interfaces:**
- Consumes: `authFetch` (Task 16)

- [ ] **Step 1: 枚举剩余直接 fetch**

Run:
```bash
grep -rn "fetch(" components/ hooks/ lib/ --include="*.ts" --include="*.tsx" \
  | grep -v "authFetch" | grep -vi "prefetch\|refetch"
```
Expected: 列出所有尚未替换的前端 `fetch(` 调用。剔除 `app/api/**`（服务端 upstream fetch，不改）与 `/api/auth/me`、`/api/auth/login`、`/api/auth/logout`（登录态本身，保留原生 fetch）。

- [ ] **Step 2: 逐个替换**

对 Step 1 剩余每处（当前快照）：
- `components/SessionSidebar.tsx`：`/api/home`(:236)
- `components/SkillsConfig.tsx`：`/api/skills/search`(:209)、`/api/skills/install`(:236)、`/api/skills`(:527、:551)
- `hooks/useAgentSession.ts`：任何 Task 16 未覆盖的剩余 fetch
- `lib/funasr.ts`：`/api/funasr/transcribe`(:39)
把 `fetch(` 改为 `authFetch(` 并加 import，保留原有 `.catch`/`try`。

- [ ] **Step 3: SSE 全量补探测**

对所有 `EventSource` 的 `onerror`（`components/FileViewer.tsx` 三处、`hooks/useAgentSession.ts`、`components/ModelsConfig.tsx` OAuth 登录流除外），补 `fetch("/api/auth/me")` 探测，401 则跳 `/login`（同 Task 16 Step 2 注记）。

- [ ] **Step 4: 验证与提交**

Run（应无输出，表示已全部替换）：
```bash
grep -rn "fetch(" components/ hooks/ lib/ --include="*.ts" --include="*.tsx" \
  | grep -v "authFetch" | grep -vi "prefetch\|refetch" \
  | grep -v "/api/auth/me\|/api/auth/login\|/api/auth/logout"
```
Expected: 无输出（除保留项）。
```bash
git add -A
git commit -m "feat: authFetch全量替换剩余前端fetch"
```

---

## Self-Review

**Spec coverage：**
- 注册关键词门 → Task 7；用户名/密码规则 → Task 2, 7；密码哈希 → Task 3；
  SQLite → Task 5；session/cookie → Task 6, 8；middleware → Task 9；
  会话过滤 → Task 10；会话详情越权 → Task 11；agent/SSE 越权 → Task 12；
  文件限制/默认 cwd/新建约束 → Task 13；删除(symlink/使用中/根防护) → Task 14；
  登录注册页(CSS 变量) → Task 15；authFetch 关键路径/顶部用户名/登出/删除按钮 → Task 16；
  端到端越权 → Task 17；端口 8033/systemd → Task 1, 18；authFetch 全量替换 → Task 19。
  全部 spec 章节均有对应任务。
- realpath 防逃逸（spec 全局约束）：文件 GET/session/agent cwd 用 `resolveExistingAndCheck`（Task 10-13），
  删除/新建用 `resolveParentAndCheck`（Task 13-14），解析失败 fail-closed。Task 4 含 symlink 逃逸单测。
- 全局 401（spec §7）：Task 16 覆盖关键路径 + SSE 探测；Task 19 全量收口。
- 已知不实现项：Agent 工具层隔离（spec §10 调研项），本计划不含，符合决策。

**类型一致性：**
- `getSessionUser(request)`、`isInsideUserRoot(target,username)`、`getUserRoot(username)`、
  `resolveExistingAndCheck`、`resolveParentAndCheck`、`hashPassword`/`verifyPassword`、`createSession`/`destroySession`/
  `readCookieToken`、`isUserRootItself`/`isPathInUse` 在定义任务与消费任务间签名一致。

**Placeholder 扫描：** 无 TBD/TODO；每个代码步骤含完整代码与预期输出。

**注意事项（执行者留意）：**
- 测试用 `node --test` 直接跑 `.ts`（Node 22 支持类型剥离）。若某环境 import `.ts` 报错，
  改用 `node --test --experimental-strip-types` 或在 test 脚本加该 flag。
- middleware 修改需重启 dev 才生效。
- 相对 import 路径（`../../../`）按实际文件层级校对。
